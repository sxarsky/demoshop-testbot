// Generated by Skyramp v1.3.16 on 2026-03-30 04:08:47.818048606 +0000 UTC m=+164.344702904
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output add_product_form_ui_test.spec.ts \
// 		--output-dir /home/runner/work/demoshop-testbot/demoshop-testbot/frontend/tests \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/demoshop-testbot/demoshop-testbot/.skyramp/add_product_form_ui_trace.zip

// Import of required libraries
import { test } from '@playwright/test';
import { expect, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test('testUi', async ({ page }) => {
    test.setTimeout(114000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:5173");
    await page.goto("http://localhost:5173/products");
    await page.getByRole("button", { name: "Add Product" }).click();

    const playwrightRequest0: Record<string, any> = {
        "category": "Electronics",
        "description": "A premium smartphone with advanced features",
        "name": "SkyPhone Pro",
        "price": 599.99
    };

    await page.getByTestId("new_product_name").fill(getValue(playwrightRequest0, "name"));
    await page.getByTestId("new_product_description").fill(getValue(playwrightRequest0, "description"));
    await expect(page.getByTestId("new_product_image_upload_btn")).toContainText("Click to upload image");
    await page.getByTestId("new_product_category").fill(getValue(playwrightRequest0, "category"));
    await page.getByRole("combobox").click();
    await page.getByTestId("new_product_instock_yes").click();
    await page.getByTestId("new_product_price").fill(getValue(playwrightRequest0, "price").toString());
    await expect(page.getByTestId("new_product_image_upload_btn")).toContainText("JPG, PNG, WEBP");
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/api/v1/products**");
    await page.getByRole("button", { name: "Submit" }).click();
    const response0 = await responsePromise0;

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    await expect(page.getByRole("button", { name: "Submit" })).toContainText("Submit");
});

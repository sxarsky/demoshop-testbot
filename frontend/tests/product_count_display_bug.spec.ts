// Generated by Skyramp v1.3.16 on 2026-03-29 03:24:29.495359182 +0000 UTC m=+248.726416468
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output product_count_display_bug.spec.ts \
// 		--output-dir /home/runner/work/demoshop-testbot/demoshop-testbot/frontend/tests \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/demoshop-testbot/demoshop-testbot/.skyramp/product_count_bug_trace.zip

// Import of required libraries
import { test } from '@playwright/test';
import { newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test('testUi', async ({ page }) => {
    test.setTimeout(269000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:5173");
    await page.goto("http://localhost:5173/products");
    await page.getByRole("textbox", { name: "Search products..." }).fill("Apple");
    await page.getByRole("combobox").click();
    await page.getByRole("option", { name: "laptop" }).click();
    await page.getByRole("button", { name: "Clear Filters" }).click();
    await page.getByRole("textbox", { name: "Search products..." }).fill("xyznonexistent");
    await page.getByRole("button", { name: "Clear Filters" }).click();
    await page.goto("http://localhost:5173/products");
    await page.getByRole("textbox", { name: "Search products..." }).fill("Sony");
    await page.getByRole("combobox").click();
    await page.getByRole("option", { exact: true, name: "headphones" }).click();
});
